using UnityEngine;

public enum RoomType {Normal, Type1,Type2,Type3 };
public enum RoomShape { Shape1, Shape2, Shape3 };
public enum PlacementRules { Rule1, Rule2, Rule3 };

public enum DistanceRules { Rule1,Rule2,Rule3 };
public class GlobalEnums : MonoBehaviour
{
    //Script for holding all the global enums at one place for easier managment
}
